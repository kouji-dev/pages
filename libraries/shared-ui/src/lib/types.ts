/**
 * Common size type for UI components (button, input, select, etc.)
 */
export type Size = 'xs' | 'sm' | 'md' | 'lg';

/**
 * Default size for UI components
 */
export const DEFAULT_SIZE: Size = 'md';
