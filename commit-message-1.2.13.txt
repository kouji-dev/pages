feat(ui): implement organization settings page (Task 1.2.13)

- Create organization settings route (`/app/organizations/:id/settings`)
- Create organization settings page component with:
  - Organization details form (name, description) with validation
  - Save changes functionality
  - Delete organization with confirmation modal
- Create delete organization confirmation modal component
- Integrate with organization update/delete APIs (using mock API interceptor)
- Implement loading, error, and empty states
- Add method to OrganizationService to fetch single organization
- Update routes to use AuthenticatedLayout for organization pages
- Update navigation paths to match new route structure

This enables users to edit and delete their organizations.

